@page
@model FitComrade.Pages.Shop.IndexModel
@{
}
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Shop</title>
</head>
<body>

    <h3>Producten</h3>
    <form>
        <p>
            Title: <input type="text" asp-for="SearchString" />
            <input type="submit" value="Filter" />
        </p>
    </form>
    <table class="table">
        <tr>
            <th>Nr.</th>
            <th>Product</th>
            <th>Foto</th>
            <th>Prijs</th>
            <th></th>

        </tr>
        @foreach (var product in Model.Products)
        {
            <tr>
                <td>@product.ID</td>
                <td>@product.ProductName</td>
                <td><img src="~/images/FitComrade logo.png" width="100" height="100" /></td>
                <td>$@product.SellPrice</td>
                <td>
                    <a asp-page="cart" asp-page-handler="buynow" asp-route-id="@product.ID">Bestel</a>
                </td>
            </tr>
        }
    </table>

</body>
</html>